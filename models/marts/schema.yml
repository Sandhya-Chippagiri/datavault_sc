version: 2

models:
  - name: dim_accounts
    columns:
      - name: ACCOUNT_HK
        tests:
          - unique
          - not_null

  - name: fct_transactions
    columns:
      - name: transaction_id
        tests:
          - unique
          - not_null
      - name: transaction_amount
        tests:
          - not_null